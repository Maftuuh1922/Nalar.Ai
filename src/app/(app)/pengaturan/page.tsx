export default function PengaturanPage() {
  return (
    <div className="p-8">
      <h1 className="mb-2 text-xl font-semibold">Pengaturan</h1>
      <p className="text-sm text-black/60 dark:text-white/60">
        Konfigurasi model AI (base URL, API key, model, embedding model) akan tersedia di sini.
      </p>
    </div>
  );
}
